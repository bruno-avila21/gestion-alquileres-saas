# .NET
bin/
obj/
*.user
*.suo
.vs/
*.nupkg
TestResults/
coverage/
*.pfx
*.p12

# Secrets locales — NUNCA commitear
api/src/GestionAlquileres.API/appsettings.Development.json
api/src/GestionAlquileres.API/appsettings.Production.json
**/secrets.json

# Node / Frontend
node_modules/
dist/
.vite/
.next/

# Claude Code settings locales
.claude/settings.local.json

# Entorno
.env
.env.local
.env.*.local

# Storage local (datos en disco, NO código).
# Las reglas van ancladas con "/" inicial: sin anclar, "storage/" matchea cualquier carpeta con ese
# nombre a cualquier profundidad y se tragaba api/src/GestionAlquileres.Infrastructure/Storage/,
# dejando LocalFileStorageService, S3StorageService y DocumentTokenService fuera del repositorio
# (un clone limpio no compilaba).
/uploads/
/storage/
/minio-data/
**/bin/uploads/

# Logs
logs/
*.log

# IDE
.idea/
.vscode/settings.json
*.swp

# Dev scripts runtime
.pids/
logs/

# Local admin/dev tools (contain dev credentials — never commit)
reset-password.csx
reset-tool/

# Downloaded scaffold / template artifacts
Template/

# Local screenshots & MCP tool output
/*.png
.playwright-mcp/

# Claude worktrees
.claude/worktrees/
